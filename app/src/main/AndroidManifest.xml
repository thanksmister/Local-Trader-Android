<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thanksmister.bitcoin.localtrader" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="6aa105ab19b074264f7333064640f494720b90f1" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--<provider
                android:name=".data.services.SyncProvider"
                android:label="Trades"
                android:syncable="true"
                android:authorities="com.thanksmister.bitcoin.localtrader.provider"
                android:exported="false"/>-->

        <provider
                android:name=".data.services.StubProvider"
                android:exported="false"
                android:syncable="true"
                android:authorities="com.thanksmister.bitcoin.localtrader.provider"/>

        <!-- Services -->
        <service android:name=".data.services.AuthenticatorService" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                    android:name="android.accounts.AccountAuthenticator"
                    android:resource="@xml/authenticator" />
        </service>
        
        <service
                android:name=".data.services.SyncService"
                android:process=":sync" >
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>

            <meta-data
                    android:name="android.content.SyncAdapter"
                    android:resource="@xml/syncadapter" />
        </service>

        <activity android:name=".ui.LoginActivity"/>
                
        <activity
            android:name=".ui.advertisements.AdvertisementActivity">
        </activity>

        <activity
                android:name=".ui.contacts.ContactActivity">
        </activity>

        <activity
                android:name=".ui.PinCodeActivity">
        </activity>

        <activity
                android:name=".ui.search.SearchResultsActivity">
        </activity>

        <activity
                android:name=".ui.advertisements.AdvertiserActivity">
        </activity>

        <activity
                android:name=".ui.search.TradeRequestActivity">
        </activity>

        <activity
                android:name=".ui.contacts.ContactsActivity">
        </activity>

        <activity
                android:name=".ui.advertisements.EditActivity">
        </activity>

        <activity
                android:name=".ui.PromoActivity">
        </activity>

        <activity
                android:name=".ui.bitcoin.QRCodeActivity"
                android:theme="@android:style/Theme.Holo.Light.Dialog.NoActionBar.MinWidth" >
        </activity>

        <activity
               android:name=".ui.bitcoin.BitcoinHandler"
               android:exported="true"
               android:label="Send Bitcoins"
               android:noHistory="true">
           <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <category android:name="android.intent.category.ALTERNATIVE" />
               <data android:scheme="bitcoin" />
           </intent-filter>
       </activity>

    </application>

</manifest>
